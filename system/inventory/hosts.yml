---
# Inventory file — run playbooks against localhost
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3
  children:
    home_desktop:
      hosts:
        localhost: {}
    # Uncomment below and comment out home_desktop to use work laptop profile
    # work_laptop:
    #   hosts:
    #     localhost: {}
