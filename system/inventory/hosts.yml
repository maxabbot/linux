---
# Inventory file — run playbooks against localhost
# Profiles are groups containing localhost as the only host
# Note: localhost variables are defined per-profile to ensure group_vars are applied correctly
all:
  vars:
    ansible_connection: local
    ansible_python_interpreter: /usr/bin/python3
  children:
    home_desktop:
      hosts:
        localhost: {}
    work_laptop:
      hosts:
        localhost: {}
