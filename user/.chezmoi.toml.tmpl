[data]
  # Machine-specific settings — chezmoi init will prompt for these
  [data.machine]
    # "desktop" or "laptop"
    type = "desktop"
    hostname = "archlinux"
    timezone = "America/New_York"

  [data.monitors]
    # Primary monitor (leave empty for auto-detect)
    primary = ""
    # Secondary monitor (leave empty if none)
    secondary = ""

  [data.wm]
    # "hyprland" or "sway"
    compositor = "hyprland"
